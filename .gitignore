# compiled output
/dist
/tmp
/out-tsc
# Only exists if Bazel was run
/bazel-out
/.scannerwork
/.sonarlint
# Local SonarQube credentials — never commit. Template: .env.sonar.example
.env.sonar
# Local-dev-only env.ts fallback values, kept out of source so Sonar's clear-text
# hotspot has nothing to flag. Template: src/utils/env.local-defaults.example.json
/src/utils/env.local-defaults.json
# Generated test-execution report; must not be committed as a stale artifact
xunit.xml
# dependencies
/node_modules
.nyc_output
coverage


# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# System Files
.DS_Store
Thumbs.db
/nodemon/nodemon-aastar-dev.json
/nodemon/nodemon-aastar-stage.json
/src/keys
/src/publicApi_v8/keys
src/keys/access_key
